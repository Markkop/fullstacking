type Product {
  id: ID!
  _id: String
  title: String
}

type Event {
  id: ID!
  _id: String
  title: String
  date: String
  description: String
  author: String
}

type Query {
  product(id: ID!): Product
  products: [Product]
  event(id: ID!): Event
  events: [Event]
}
